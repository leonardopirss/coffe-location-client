name: Main Workflow

on:
  push:
    branches:
      - develop
      - main
  pull_request:
    branches:
      - develop
      - main

jobs:
  call-node-workflow:
    uses: leonardopirss/workflow/.github/workflows/node.yml@develop
    with:
      node-version: '18'
    secrets: inherit
